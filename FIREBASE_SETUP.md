# Firebase로 옮기기 — 처음부터 끝까지 따라하기

이 문서만 보고 그대로 따라 하면 됩니다. 개발 지식이 없어도 진행할 수 있도록
클릭할 버튼 이름까지 최대한 그대로 적었습니다(Firebase 화면이 업데이트되면
버튼 위치나 문구가 살짝 바뀔 수 있지만, 순서와 개념은 동일합니다).

막히는 단계가 있으면 그 단계 번호와 화면에 보이는 내용을 그대로 알려주시면
같이 해결하겠습니다.

## 현재 상태: 배포 완료, 정상 동작 중

card 프로젝트는 아래 과정을 이미 전부 마치고 실제로 쓰고 있는 상태입니다.

- **실제 주소**: <https://somi-neo-19.github.io/card/> — 홈 화면에 추가해서 앱처럼 쓰시면 됩니다.
- `index.html`에는 이미 실제 firebaseConfig 값이 채워져 있어서 **더 이상 값을
  입력하실 필요가 없습니다** (아래 5단계 참고용으로 실제 값을 남겨뒀습니다).
- Firestore에 실제 카드 데이터(토스하나카드 등)를 넣어보고 다른 기기에서도
  똑같이 보이는 것까지 확인 완료됐습니다.

아래 1~9단계는 **처음부터 다시 만들 때(예: 아이스하키 프로젝트를 같은 방식으로
옮길 때) 그대로 따라 하는 절차**로 남겨둔 것입니다. card 프로젝트 자체는
다시 할 필요가 없습니다.

---

## 미리 알아둘 것: 설치할 프로그램이 없습니다

우리가 만드는 건 앱스토어에 올리는 아이폰 앱이 아니라, **모바일 브라우저(사파리/크롬)로
여는 웹페이지**입니다. 그래서 **Xcode, Node.js, 개발자 도구 같은 걸 설치할 필요가
전혀 없습니다.** 아래 두 웹사이트만 브라우저로 열면 됩니다:

1. **console.firebase.google.com** — 설정값을 받는 곳
2. **github.com** — 파일을 올리고, 필요하면 그 자리에서 바로 수정하는 곳

`index.html` 파일을 고치는 것도 컴퓨터에 편집기를 깔 필요 없이, **GitHub 웹사이트
안에서 직접 편집**할 수 있습니다(아래 8단계에서 방법 안내). 컴퓨터/휴대폰
브라우저만 있으면 전 과정이 끝납니다.

---

## 0. 지금까지 무엇이 바뀌었는지

`index.html` 안의 데이터 저장 방식을 Claude 아티팩트 전용 저장소에서
**Firebase(Firestore)** 로 바꿨습니다. 화면 디자인, 카드 계산 로직 등
기능은 전혀 바뀌지 않았고, "어디에 저장하느냐"만 바뀌었습니다.

- 기존에 드렸던 Claude 아티팩트 링크는 이 작업과 무관하게 그대로 살아있습니다.
- 아이스하키 프로젝트도 나중에 같은 방식(Firestore)으로 옮기고 싶다고
  하셨는데, 이 문서의 1~3단계(프로젝트 생성, Firestore 켜기, 규칙 설정)는
  그 프로젝트에도 똑같이 적용할 수 있습니다. 실제 이전 작업은 별도로
  요청해주시면 진행하겠습니다.

---

## 1. Firebase 프로젝트 만들기

1. 구글 계정으로 [console.firebase.google.com](https://console.firebase.google.com) 접속
2. **"프로젝트 추가"** (또는 "Add project") 클릭
3. 프로젝트 이름 입력 (예: `card-hurdle-dashboard`) → 계속
4. "이 프로젝트에서 Google 애널리틱스 사용" 옵션은 **꺼도 됩니다** (개인용 앱이라
   불필요합니다) → **"프로젝트 만들기"** 클릭
5. 몇 초 기다리면 "새 프로젝트가 준비되었습니다" 화면이 뜹니다 → **계속**

---

## 2. Firestore 켜기 (실제 데이터가 저장되는 곳)

Firebase 콘솔 화면이 업데이트되면서 왼쪽 메뉴 구성이 바뀌었을 수 있습니다.
아래 두 가지 중 눈에 보이는 쪽으로 진행하면 됩니다.

- 왼쪽 메뉴에 **"빌드(Build)"** 가 보이면: **"빌드" → "Firestore Database"** 클릭
- 왼쪽 메뉴가 카테고리별로 묶여 있고 "빌드"가 안 보이면:
  **"데이터베이스 및 스토리지"** 클릭(펼쳐짐) → 목록에서 **"Firestore"** 클릭
  (메뉴 이름이 "Firestore Database"가 아니라 그냥 "Firestore"로 짧게 표시될
  수 있습니다 — 같은 서비스입니다. "Realtime Database"는 이름이 비슷한
  다른 옛날 제품이니 헷갈리지 마세요.)

1. 위 방법으로 Firestore 화면에 들어간 뒤 **"데이터베이스 만들기(Create database)"** 클릭
2. 위치(Location)는 가까운 지역으로 선택 (예: `asia-northeast3 (Seoul)`) → 다음
   - ⚠️ 위치는 나중에 바꿀 수 없으니 한 번에 정확히 선택해주세요.
3. 보안 규칙 모드는 아무거나 선택해도 됩니다 (어차피 3단계에서 직접 교체합니다)
   → **사용 설정(Enable)** 클릭

---

## 3. 보안 규칙 붙여넣기 (아무나 내 데이터를 못 보게 막는 설정)

1. Firestore 화면에서 상단 탭 중 **"규칙(Rules)"** 클릭
2. 편집창에 있는 기존 내용을 전부 지우고, 이 프로젝트 폴더에 있는
   **`firestore.rules`** 파일의 내용을 그대로 복사해서 붙여넣기
3. 오른쪽 위 **"게시(Publish)"** 클릭

이 규칙은 "로그인한 사람만 읽고 쓸 수 있다"는 최소한의 보호 장치입니다.
아래 4단계에서 만드는 "익명 로그인"이 바로 그 로그인 역할을 합니다
(비밀번호 입력 화면은 뜨지 않고, 앱이 열릴 때 자동으로 조용히 로그인됩니다).

---

## 4. 익명 로그인 켜기

여기도 마찬가지로 메뉴 위치가 다를 수 있습니다.

- **"빌드(Build)"** 가 보이면: **"빌드" → "Authentication"** 클릭
- 카테고리별 메뉴라면: **"DevOps 및 사용자 참여"** 클릭(펼쳐짐) →
  **"Authentication"** 클릭

1. 위 방법으로 Authentication 화면에 들어간 뒤 **"시작하기(Get started)"** 클릭 (처음 한 번만 나옵니다)
2. **"Sign-in method"** 탭 → 로그인 제공업체 목록에서 **"익명(Anonymous)"** 클릭
3. 스위치를 **켜짐(Enable)** 으로 바꾸고 **저장(Save)**

---

## 5. 웹 앱 등록하고 설정값 복사하기

1. 왼쪽 위 톱니바퀴 아이콘 → **"프로젝트 설정(Project settings)"** 클릭
2. 아래로 스크롤하면 나오는 **"내 앱(Your apps)"** 섹션에서
   `</>` 모양(웹) 아이콘 클릭
3. 앱 닉네임 입력 (예: `card-web`) → **"Firebase Hosting도 설정"** 체크는
   **하지 않아도 됩니다** (GitHub Pages로 배포할 예정이므로) → **앱 등록**
4. 화면에 아래와 같은 코드가 나타납니다. **이 값들만** 필요합니다.

card 프로젝트는 아래 실제 값으로 이미 설정 완료된 상태입니다(참고/백업용으로 남겨둠):

```js
const firebaseConfig = {
  apiKey: "AIzaSyAWcUVOmTx3en_spvGcgfLEY4I7MumcxPQ",
  authDomain: "card-9c857.firebaseapp.com",
  projectId: "card-9c857",
  storageBucket: "card-9c857.firebasestorage.app",
  messagingSenderId: "486812990457",
  appId: "1:486812990457:web:f706ebf5ca33a462429b28"
};
```

(참고: 이 값들은 브라우저에 그대로 노출되도록 설계된 값이라 공개돼도
안전합니다 — 실제 데이터 보호는 3단계의 Firestore 보안 규칙이 담당합니다.)

아이스하키 프로젝트 등 **새 프로젝트를 만들 때는** 여기서 나오는 6개 값을
그대로 복사해두었다가 8단계에서 그 프로젝트의 `index.html`에 붙여넣으면 됩니다.

---

## 6. 다운로드한 파일을 GitHub에 올리기 (컴퓨터 프로그램 설치 없이, 웹사이트에서만)

card 프로젝트는 이 과정을 이미 마쳤습니다. 새 프로젝트를 만들 때 참고하세요.

전달받은 `index.html`, `firestore.rules`, `FIREBASE_SETUP.md` 3개 파일을
GitHub 저장소에 올립니다. 편집기 없이 웹사이트 화면만으로 진행합니다.

1. 브라우저로 저장소 페이지를 엽니다.
2. **"Add file"** 버튼(파일 목록 위쪽) → **"Upload files"** 클릭
3. 파일들을 화면에 끌어다 놓습니다(또는 "choose your files"로 선택)
   - `index.html`은 같은 이름의 기존 파일을 **덮어씁니다** — 정상입니다.
4. 아래로 스크롤 → 커밋 메시지는 그대로 두거나 "Firebase 설정 추가" 정도로
   입력 → **"Commit changes"** 클릭

이 시점에는 아직 `firebaseConfig` 안에 "여기에 ~~ 붙여넣으세요"라는 안내
문구가 그대로 들어있는 상태라, 페이지를 열어도 안내 배너만 뜹니다. 정상입니다.

---

## 7. GitHub Pages로 공개하기

card 프로젝트는 이미 아래 주소로 공개되어 있습니다.

**→ <https://somi-neo-19.github.io/card/>**

새 프로젝트를 만들 때는 이렇게 진행합니다:

1. 저장소 페이지 상단 **"Settings"** 탭 클릭
2. 왼쪽 메뉴에서 **"Pages"** 클릭
3. **"Branch"** 를 `main`, 폴더는 `/ (root)` 로 선택 → **Save**
4. 1~2분 후 페이지 새로고침하면 `https://<계정명>.github.io/<저장소이름>/` 같은
   주소가 화면에 나타납니다. 이 주소를 저장해두세요 — 앞으로 계속 쓸 주소입니다.

---

## 8. index.html에 설정값 붙여넣기 (GitHub 웹사이트에서 바로 수정)

card 프로젝트는 이미 완료됐습니다. 새 프로젝트를 만들 때 참고하세요.

컴퓨터에 아무것도 설치하지 않고, GitHub 사이트 안에서 바로 고칩니다.

1. 저장소 페이지에서 `index.html` 파일 이름을 클릭해서 엽니다.
2. 오른쪽 위 **연필 아이콘(✏️, "Edit this file")** 클릭
3. `Ctrl+F`(맥은 `Cmd+F`)로 **"여기를 수정하세요"** 를 검색하면 아래 블록이
   바로 보입니다:

   ```js
   var firebaseConfig = {
     apiKey: "여기에 apiKey 값을 붙여넣으세요",
     authDomain: "여기에 authDomain 값을 붙여넣으세요",
     projectId: "여기에 projectId 값을 붙여넣으세요",
     storageBucket: "여기에 storageBucket 값을 붙여넣으세요",
     messagingSenderId: "여기에 messagingSenderId 값을 붙여넣으세요",
     appId: "여기에 appId 값을 붙여넣으세요"
   };
   ```

4. 5단계에서 복사해둔 값으로 큰따옴표(`"..."`) **안쪽 문구만** 교체합니다
   (큰따옴표 자체는 지우지 마세요). 6줄 전부 바꿔야 합니다.
5. 화면 아래로 스크롤 → **"Commit changes"** 클릭 (제목 그대로 두어도 됩니다)
6. 30초~1분 정도 기다린 뒤, 7단계에서 저장해둔 GitHub Pages 주소를 다시
   열어보세요(캐시 때문에 안 바뀐 것처럼 보이면 새로고침을 한 번 더 해보세요,
   또는 앱 안의 "당겨서 새로고침"으로 강제로 최신 버전을 받아올 수 있습니다).
   홈 화면에 추가하면 아이콘을 눌러 앱처럼 바로 열 수 있습니다
   (iOS: 공유 버튼 → "홈 화면에 추가" / Android: 브라우저 메뉴 → "홈 화면에 추가").

---

## 9. 정상 동작 확인

card 프로젝트는 이미 확인 완료됐습니다(실제 카드 데이터를 넣고 다른 기기에서도
똑같이 보이는 것까지 검증함).

1. 배포된 주소를 열면 처음엔 데이터가 없어서 KB국민/하나 클럽SK 카드가
   자동으로 한 번 생성됩니다(기존과 동일한 씨드 데이터).
2. 카드 정보를 입력하고, 다른 기기(PC/폰)에서 같은 주소를 열어봤을 때
   방금 입력한 내용이 그대로 보이면 정상 동작하는 것입니다.

---

## 10. Firebase 이전 이후 추가된 기능들

배포 이후 아이폰 사용성 관련해서 아래 기능들을 추가로 반영했습니다.
모두 `index.html` 안에서만 바뀐 내용이라 Firebase 콘솔에서 다시 할 일은 없습니다.

- **뷰포트(모바일 화면 크기) 설정 추가**: 원래 이 태그가 빠져 있어서 폰에서
  전체가 축소되어 보이고 카드 2개가 한 줄에 끼어 보이던 문제를 해결했습니다.
- **모바일 화면(640px 이하)에서 카드 1줄에 1개씩** 표시, 전체적으로 글씨 크기 확대.
- **포인트 결제 항목 안내 문구**("포인트 OO원 차감 후 0원 결제 예정")가 세로로
  깨져 보이던 CSS 버그 수정.
- **정기결제 입력란을 모바일에서 2줄로 재배치**(날짜/항목명/삭제 → 종류/금액)해서
  금액 입력란이 더 넓고 잘 보이도록 개선.
- **금액 입력란에 세자리 콤마 자동 표시**(허들 금액, 현재 실적, 정기결제 금액,
  추천받기 금액 전부).
- **당겨서 새로고침**: 화면 맨 위에서 아래로 당기면 캐시를 무시하고 최신
  버전을 강제로 다시 받아옵니다.

---

## 문제 해결

| 화면에 뜨는 문구 | 원인 | 해결 |
|---|---|---|
| "firebaseConfig 값이 아직 채워지지 않았습니다" | 8단계를 아직 안 했거나 값이 남아있음 | index.html 상단 firebaseConfig 6개 값을 다시 확인 |
| "로그인에 실패했습니다..." | 4단계(익명 로그인 켜기)를 안 함 | Firebase 콘솔 Authentication에서 익명 로그인이 켜져 있는지 확인 |
| "데이터를 불러오지 못했습니다..." | 3단계(보안 규칙)가 적용 안 됐거나 오타 | Firestore 콘솔 Rules 탭에서 `firestore.rules` 내용이 정확히 붙여넣어졌는지, "게시"를 눌렀는지 확인 |
| 값을 입력했는데 새로고침하면 사라짐 | 위 항목들과 동일 (저장이 실패하고 있는 것) | 브라우저 개발자 도구(F12) → Console 탭에 빨간 에러 메시지가 있으면 캡처해서 알려주세요 |

---

## 무료 사용량 안내 (Firebase Spark 플랜)

개인/가족 단위로 쓰는 정도로는 무료 한도를 넘길 일이 거의 없습니다.

- Firestore 저장 용량 1GB, 하루 읽기 5만 회 / 쓰기 2만 회 / 삭제 2만 회
- Authentication 익명 로그인: 무료, 개수 제한 없음
- 카드 사용 실적 기록 같은 소규모 개인 앱은 하루 수십~수백 회 수준이라
  이 한도의 극히 일부만 씁니다.

---

## 참고: 이 방식의 보안 수준

익명 로그인 + "로그인한 사람만 읽기/쓰기 가능" 규칙은 완전 공개보다는
안전하지만, 은행 앱 수준의 보안은 아닙니다. 카드 번호나 비밀번호 같은
민감정보를 저장하지 않는 개인용 실적 관리 앱이라는 전제 하에 적절한
수준으로 설정했습니다. 더 엄격한 보호(이메일 로그인 등)가 필요하시면
말씀해주세요.
